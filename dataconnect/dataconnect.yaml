specVersion: "v1beta"
serviceId: "qtmcursor"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "qtmcursor-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
